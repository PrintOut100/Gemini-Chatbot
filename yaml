services:
  - name: web
    env:
      - key: RENDER_ENVIRONMENT
        value: production
    buildCommand: |
      pip install -r requirements.txt
      cp .env $HOME/.env
    startCommand: python app.py
